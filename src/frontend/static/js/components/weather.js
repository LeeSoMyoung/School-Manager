'use strict';

require('dotenv').config();

const WEATHER_API_KEY=process.env.WEATHER_API_KEY;
const WEATHER = "weather";
const GEOLOACTION = "geoloaction";

function weatherError(){
    // geolocation을 불러오는 데 실패했을 시
}

function getWeather(){
    // geolocation을 불러오는 데 성공했을 시

}